<%=link_to("Back to List ",{:action =>'list' },:class =>'back-list') %>

<div class="section delete">
   <h2>Delete Section </h2>
   
   <%=form_for(:page ,:url => {:action =>'destroy',:id=>@section.id}) do |f|  %>
   
   <p>Are u sure you want to delete the page?  </p>
   <p class="reference-name"><%= @section.name %></p>
   
   <div class="form-buttons">
   <%= submit_tag("Delete Section")  %>
   </div>
   
   <% end %>
   </div>
   
